import React from "react";

export default function UserProfile(){
    //will fetch information from backend using token. Need to update the backend first
    return (
        <>
            hello authenticated user!
        </>
    )
}